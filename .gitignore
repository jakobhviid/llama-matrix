/target
# cargo output from the containerised build (this host has no cc)
/target-container
.claude/
.DS_Store
*.swp

# Internal spec/reference kit — private box details (hostnames, IPs, real model
# roster). Kept locally to build from; never published.
/specdocs
